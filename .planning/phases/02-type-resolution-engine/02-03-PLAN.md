---
phase: 02-type-resolution-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - sidecar/src/handlers/resolve.ts
  - sidecar/src/handlers/resolve.test.ts
  - sidecar/src/main.ts
  - sidecar/tsup.config.ts
autonomous: true
requirements: [TRES-01, SIDE-03, SIDE-04, SIDE-06]

must_haves:
  truths:
    - "Sending a resolve request over NDJSON returns a structured TypeNode tree"
    - "The resolve handler validates params (filePath, position) and returns clear errors"
    - "The sidecar builds successfully with typescript bundled"
    - "End-to-end: spawn sidecar, send resolve request, receive TypeNode response"
  artifacts:
    - path: "sidecar/src/handlers/resolve.ts"
      provides: "Resolve handler entry point wiring params to type-walker"
      exports: ["handleResolve"]
    - path: "sidecar/src/handlers/resolve.test.ts"
      provides: "End-to-end integration tests via NDJSON protocol"
    - path: "sidecar/src/main.ts"
      provides: "Updated dispatch with resolve case"
      contains: "case \"resolve\""
  key_links:
    - from: "sidecar/src/handlers/resolve.ts"
      to: "sidecar/src/services/type-walker.ts"
      via: "calls resolveAtPosition"
      pattern: "resolveAtPosition"
    - from: "sidecar/src/handlers/resolve.ts"
      to: "sidecar/src/types.ts"
      via: "uses ResolveParams, ResolveResult"
      pattern: "ResolveParams|ResolveResult"
    - from: "sidecar/src/main.ts"
      to: "sidecar/src/handlers/resolve.ts"
      via: "switch case dispatch"
      pattern: "handleResolve"
---

<objective>
Wire the type resolution into the NDJSON protocol by creating the resolve handler, adding it to main.ts dispatch, and verifying end-to-end with integration tests.

Purpose: This is the integration layer that makes the type walker accessible over the sidecar protocol. After this plan, Neovim can send `{method: "resolve", params: {filePath, position}}` and get back a TypeNode tree.
Output: Working resolve handler, updated main.ts, passing integration tests, successful build.
</objective>

<execution_context>
@/Users/osvi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/osvi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-type-resolution-engine/02-RESEARCH.md
@.planning/phases/02-type-resolution-engine/02-CONTEXT.md
@.planning/phases/02-type-resolution-engine/02-01-SUMMARY.md
@.planning/phases/02-type-resolution-engine/02-02-SUMMARY.md

<interfaces>
<!-- From plan 01 -- types for request/response -->
From sidecar/src/types.ts:
```typescript
export interface ResolveParams {
  filePath: string;
  position: number;
}
export interface ResolveResult {
  node: TypeNode | null;
}
```

<!-- From plan 02 -- walker entry point -->
From sidecar/src/services/type-walker.ts:
```typescript
export function resolveAtPosition(filePath: string, position: number, timeoutMs?: number): ResolveResult;
```

<!-- Existing dispatch pattern -->
From sidecar/src/main.ts:
```typescript
switch (msg.method) {
  case "echo":
    result = handleEcho(msg.params);
    break;
  default: // UNKNOWN_METHOD error
}
```

From sidecar/src/protocol.ts:
```typescript
export interface Request { id: number; method: string; params: unknown; }
export interface Response { id: number; result: unknown; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve handler and main.ts wiring</name>
  <files>sidecar/src/handlers/resolve.ts, sidecar/src/handlers/resolve.test.ts, sidecar/src/main.ts</files>
  <behavior>
    - Test 1 (integration): Spawn sidecar, send `{id:1, method:"resolve", params:{filePath:"test-fixtures/simple.ts", position: POS_OF_USER}}` over stdin, receive response with TypeNode of kind "object" with property children
    - Test 2 (integration): Send resolve for a primitive (`num` in simple.ts), receive kind "primitive"
    - Test 3 (integration): Send resolve for a union type (`Status` in simple.ts), receive kind "union" with branch children
    - Test 4 (integration): Send resolve for a function (`greet` in simple.ts), receive kind "function" with param children
    - Test 5 (unit): handleResolve with missing filePath returns error
    - Test 6 (unit): handleResolve with missing position returns error
  </behavior>
  <action>
Create `sidecar/src/handlers/resolve.ts`:
- Export `handleResolve(params: unknown): ResolveResult`
- Validate params: must have `filePath` (string) and `position` (number), throw with descriptive message if missing
- Resolve filePath to absolute path using `path.resolve`
- Call `resolveAtPosition(absolutePath, position)` from type-walker
- Return the ResolveResult directly

Update `sidecar/src/main.ts`:
- Import `handleResolve` from `./handlers/resolve.js`
- Add `case "resolve": result = handleResolve(msg.params); break;` to the switch

Write integration tests in `sidecar/src/handlers/resolve.test.ts`:
- Follow the same pattern as existing protocol.test.ts (spawn child process, write NDJSON to stdin, read response from stdout)
- Use the test-fixtures/ directory for fixture paths (resolve to absolute paths in tests)
- For position values: read the fixture file content, find the position of specific identifiers using string indexOf
- Test that the response has the expected TypeNode structure (kind, name, children existence)

After implementation, run build: `npm run build` to verify tsup bundles correctly with typescript included (the bundle will be larger since typescript is now a dependency -- this is expected per research recommendation to keep bundling).
  </action>
  <verify>
    <automated>cd /Users/osvi/src/typescript-explorer.nvim-restart/sidecar && npx tsx --test src/handlers/resolve.test.ts && npm run build</automated>
  </verify>
  <done>Resolve handler accepts {filePath, position}, returns TypeNode tree over NDJSON protocol. main.ts dispatches "resolve" method. Integration tests verify end-to-end round-trip for object, primitive, union, and function types. Build succeeds with typescript bundled.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite validation and build verification</name>
  <files>sidecar/tsup.config.ts</files>
  <action>
Run the complete test suite across all test files to verify nothing is broken:
```bash
cd sidecar && npx tsx --test src/**/*.test.ts
```

Verify the built output works by running the sidecar from dist/main.js:
```bash
echo '{"id":1,"method":"resolve","params":{"filePath":"PATH_TO_SIMPLE_TS","position":POS}}' | node dist/main.js
```
(Use absolute path to test-fixtures/simple.ts and position of the `user` identifier)

If tsup build fails due to typescript size or bundling issues, adjust `sidecar/tsup.config.ts` if needed -- but per research, `noExternal: [/.*/]` should handle it fine.

Ensure all existing tests (protocol.test.ts, lifecycle.test.ts) still pass alongside the new tests.
  </action>
  <verify>
    <automated>cd /Users/osvi/src/typescript-explorer.nvim-restart/sidecar && npx tsx --test src/**/*.test.ts && npm run build && echo '{}' | timeout 5 node dist/main.js 2>/dev/null || true</automated>
  </verify>
  <done>All tests pass (language-service, type-walker, resolve handler, protocol, lifecycle). Build produces dist/main.js that can resolve types. Sidecar is ready for Phase 3 panel integration.</done>
</task>

</tasks>

<verification>
- `cd sidecar && npx tsx --test src/**/*.test.ts` -- full suite green
- `cd sidecar && npm run build` -- builds without errors
- Send resolve NDJSON to built sidecar, receive TypeNode response
- All Phase 1 tests still pass (no regressions)
</verification>

<success_criteria>
- "resolve" method registered in sidecar's NDJSON dispatch
- Sending `{method: "resolve", params: {filePath, position}}` returns a TypeNode tree
- Object, union, function, primitive types all resolve correctly end-to-end
- Invalid params return HANDLER_ERROR with descriptive message
- Build succeeds and dist/main.js works
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-type-resolution-engine/02-03-SUMMARY.md`
</output>
