---
phase: 03-panel-ui-and-integration
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - sidecar/src/services/type-walker.ts
  - sidecar/src/services/type-walker.test.ts
autonomous: false
gap_closure: true
requirements:
  - PANE-02
  - PANE-03
  - PANE-06

must_haves:
  truths:
    - "Resolving `parsed` variable returns Result<number, Error>, not the parse function type"
    - "Resolving `bus` returns a valid type tree (panel updates, not blank)"
    - "Resolving TypeCheckEvent.diagnostics shows Diagnostic[] with expandable children"
  artifacts:
    - path: "sidecar/src/services/type-walker.ts"
      provides: "Corrected type walker (if residual fixes needed)"
    - path: "sidecar/src/services/type-walker.test.ts"
      provides: "Tests confirming GAP-04/05/06 are resolved"
  key_links:
    - from: "sidecar/src/services/type-walker.ts"
      to: "resolveAtPosition"
      via: "correct symbol resolution for variables near overloaded functions"
      pattern: "resolveAtPosition"
---

<objective>
Re-verify and fix cascading gaps GAP-04 (parsed variable type), GAP-05 (slow/no panel update on complex types), and GAP-06 (diagnostics shows as {} with no children). These were hypothesized to cascade from GAP-01 (stack overflow). After GAP-01 is fixed in plan 03-03, re-test and fix any residual issues.

Purpose: Confirm cascading gaps are resolved or apply targeted fixes for any that persist.
Output: All 6 gaps closed, full test coverage for edge cases.
</objective>

<execution_context>
@/Users/osvi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/osvi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-panel-ui-and-integration/03-GAPS.md
@.planning/phases/03-panel-ui-and-integration/03-03-SUMMARY.md

@sidecar/src/services/type-walker.ts
@sidecar/src/services/type-walker.test.ts
@sidecar/src/types.ts
@test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify and fix GAP-04/05/06 after stack overflow fix</name>
  <files>sidecar/src/services/type-walker.ts, sidecar/src/services/type-walker.test.ts</files>
  <action>
    Run targeted resolution tests against test.ts to check if GAP-04/05/06 are resolved after GAP-01 fix:

    **GAP-04 check:** Use `resolveAtPosition` on the `parsed` variable (line 117 of test.ts: `const parsed = parse("42")`). Find the byte position of the identifier `parsed`. The result should have typeString containing "Result" (the variable's type), NOT a function signature. If it still shows function type, the issue is in `resolveAtPosition`'s symbol lookup -- the cursor may be landing on the `parse` function call instead of the `parsed` variable binding. Fix by ensuring `findTokenAtPosition` returns the declaration identifier, not the initializer expression.

    **GAP-05 check:** Use `resolveAtPosition` on `bus` (line 113) and `handler` (line 114). After GAP-01 fix, these should return valid TypeNode trees without crashing or timing out. If `bus` still times out, the maxDepth guard from 03-03 may need tuning (try depth 10 instead of 15, or apply special handling for class constructor types).

    **GAP-06 check:** Use `resolveAtPosition` on `TypeCheckEvent` (line 29), then check if the `diagnostics` child has children of its own (should be Diagnostic[] with expandable array element). If it shows as empty object `{}`, the issue is that the array's element type properties are not being walked. Verify the array branch in walkType correctly recurses into the element type.

    Write tests for each gap:
    ```typescript
    describe("gap closure", () => {
      it("GAP-04: parsed variable resolves to Result type, not function", () => {
        // Find "parsed" in test.ts, assert typeString includes "Result"
      });
      it("GAP-05: bus resolves without crash", () => {
        // Find "bus" in test.ts, assert result.node is non-null
      });
      it("GAP-06: TypeCheckEvent diagnostics has array children", () => {
        // Resolve TypeCheckEvent, find diagnostics child, assert it has children
      });
    });
    ```

    Use the project root `test.ts` file as the fixture (it already contains all the reproducer types). The fixture helper `fixturePos` currently looks in `test-fixtures/` -- either add a helper that reads from project root, or copy the relevant types into a new fixture file `sidecar/test-fixtures/complex.ts`.

    If any gap persists after re-verification, apply targeted fixes:
    - GAP-04: Likely a byte offset issue in test, not a walker bug. If `resolveAtPosition` returns function type for `parsed`, check that the position lands on `parsed` not `parse`.
    - GAP-05: If still timing out, reduce maxDepth or add early termination for class constructor types.
    - GAP-06: If diagnostics still empty, check that the walker follows the array element type into its properties.
  </action>
  <verify>
    <automated>cd sidecar && npx tsx --test src/services/type-walker.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `parsed` resolves to Result type (GAP-04 closed)
    - `bus` resolves without crash or timeout (GAP-05 closed)
    - `TypeCheckEvent.diagnostics` shows as Diagnostic[] with children (GAP-06 closed)
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 6 gaps closed in live Neovim</name>
  <files>test.ts</files>
  <action>
    Human verifies all 6 type-walker gaps are resolved in a live Neovim session with the plugin loaded.
  </action>
  <verify>Human confirms panel displays correct types for all test.ts symbols</verify>
  <done>All 6 gaps approved by user in live testing</done>
  <what-built>Fixed all 6 type-walker gaps: stack overflow protection (GAP-01), overload signatures (GAP-02), private members (GAP-03), and cascading issues (GAP-04/05/06)</what-built>
  <how-to-verify>
    1. Open Neovim on test.ts with the plugin loaded (`:TsExplorer` to open panel)
    2. Place cursor on `bus` (line 113) -- panel should show EventBus type tree without crashing
    3. Place cursor on `parsed` (line 117) -- panel should show `Result<number, Error>` type, not parse function
    4. Place cursor on `TypeCheckEvent` (line 29) -- expand `diagnostics` node, should show `Diagnostic[]` with children
    5. Place cursor on `parse` (line 56) -- should show overload signatures as children
    6. Place cursor on `handler` (line 114) -- panel should update (not stall)
    7. Check `:messages` for any errors -- should be clean
  </how-to-verify>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
Run full test suite: `cd sidecar && npm test`
All tests pass including gap closure tests.
Manual verification in Neovim confirms all 6 gaps are closed.
</verification>

<success_criteria>
- All 6 gaps from 03-GAPS.md are closed
- No sidecar crashes on any symbol in test.ts
- Panel updates correctly for all test.ts symbols
- Human verification confirms end-to-end behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-panel-ui-and-integration/03-04-SUMMARY.md`
</output>
